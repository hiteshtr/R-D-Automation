extends ../layouts/default

block main
  center
      h2|Faculty
block content
  center
    h4|List of Faculties  

  .table-responsive
    table(class='table table-striped table-bordered table-condensed')
      thead
        tr
          th #
          th Faculty Name
          th Department
          th Designation
          th
          th
      tbody   
        - each faculty,i in faculties
          tr
            td
              form( method="post", action="/faculty/#{faculty._id}") 
               input(type='hidden', name="_method", value="delete") 
               input(type='hidden', name='_csrf', value="#{csrf_token}")
               input(name="_id", type="hidden", value=faculty._id.toHexString()) 
               button.btn.btn-default.glyphicon.glyphicon-trash(type='submit',onclick="return confirm('Are you sure to delete?')")
                  
            td
              .form-control(type='text', name="faculty_name",value=faculty.faculty_name, contentEditable='false',id='faculty_name#{i}')= faculty.faculty_name
            td
              - each department in departments
                if department._id == faculty.faculty_dept_id
                  div.control-label= department.department
            td  
             select.form-control(type='text', disabled='true',id='post_name#{i}')
               if faculty.faculty_desig_id == "0"
                  option(value=0, selected) (select)
               else
               - each post in posts
                 if faculty.faculty_desig_id == post._id
                  option(value=post._id, selected)= post.post_name
                 else
                  option(value=post._id)= post.post_name
            td
               input(name="_id", type="hidden", value=faculty._id)
               button.btn.btn-default(onclick="enablefaculty('update#{i}','faculty_name#{i}','post_name#{i}')") Edit
            td
              form( method="post", action="/faculty/#{faculty._id}") 
               input(type='hidden', name="_method", value="put") 
               input(type='hidden', name='_csrf', value="#{csrf_token}")
               input(name="_id", type="hidden", value=faculty._id) 
               button.btn.btn-default(type='submit', disabled='true',id='update#{i}',,onclick="return confirm('Are you sure to Update?')") Update
            